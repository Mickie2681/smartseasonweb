{
  "installCommand": "npm install",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/:path((?!.*\\..*|_next).*)",
      "destination": "/index.html"
    }
  ]
}
